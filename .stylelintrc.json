{
    "extends": "stylelint-config-standard",
    "rules": {
        "selector-class-pattern": [
            "^((sd|sv)-.+|[a-z][a-z0-9]*(-[a-z0-9]+)*)$",
            {
                "message": "Expected class selector to be kebab-case (vendor classes sd-* sv-* are exempt)"
            }
        ]
    }
}
